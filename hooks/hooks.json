{
  "description": "openclone — inject active clone persona on every user prompt so Claude responds as the selected clone",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/hooks/inject-active-clone.sh",
            "timeout": 5
          }
        ]
      }
    ]
  }
}
