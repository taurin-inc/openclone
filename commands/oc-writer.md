---
description: Writer panel — ask a question and get perspectives from every writer-category clone side by side
argument-hint: "<question>"
allowed-tools: Read, Glob, Bash
---

Run the category panel workflow with **category = `writer`** and question = `$ARGUMENTS`.

Load `${CLAUDE_PLUGIN_ROOT}/references/panel-workflow.md` and `${CLAUDE_PLUGIN_ROOT}/references/categories.md` (for this category's "Always checks" axes). Follow the panel workflow exactly.
